# Red Ledger MVP — 테스트 체크리스트

작성일: 2026-06-16

## 1. 실행 테스트

| 체크 | 항목 | 기준 |
|---|---|---|
| [ ] | `npm install` | 의존성 설치 완료 |
| [ ] | `npm run dev` | 개발 서버 실행 |
| [ ] | 브라우저 접속 | `http://localhost:5173` 접속 가능 |
| [ ] | `npm run build` | TypeScript + Vite 빌드 성공 |

## 2. 화면 테스트

| 체크 | 항목 | 기준 |
|---|---|---|
| [ ] | 타이틀 표시 | `적혈의 장부`가 보임 |
| [ ] | 자동 저장 표시 | `자동 저장 / localStorage` 표시가 보임 |
| [ ] | 자원 카드 표시 | 8개 핵심 자원이 보임 |
| [ ] | 이벤트 패널 표시 | 현재 Day 이벤트가 보임 |
| [ ] | 선택지 표시 | A/B 선택지가 보임 |
| [ ] | 주요 인물 표시 | 주요 인물 5명이 보임 |
| [ ] | 작업 기록 표시 | 진행 기록이 보임 |
| [ ] | 새 운영 시작 버튼 | 초기화 버튼이 보임 |

## 3. 게임 로직 테스트

| 체크 | 항목 | 기준 |
|---|---|---|
| [ ] | 선택지 A 클릭 | 자원 변화 적용 |
| [ ] | 선택지 B 클릭 | 자원 변화 적용 |
| [ ] | 일차 진행 | Day 1에서 Day 7까지 진행 |
| [ ] | 자원 범위 제한 | 최소/최대값을 넘지 않음 |
| [ ] | 엔딩 판정 | 7일차 이후 엔딩 표시 |
| [ ] | 다시 운영하기 | 초기 상태로 리셋 |

## 4. 저장/불러오기 테스트

| 체크 | 항목 | 기준 |
|---|---|---|
| [ ] | 자동 저장 | 선택 후 localStorage에 상태 저장 |
| [ ] | 새로고침 복원 | 새로고침 후 진행 상태 유지 |
| [ ] | 새 운영 시작 | 저장 데이터 삭제 후 초기 상태 복귀 |
| [ ] | 손상 데이터 처리 | 비정상 저장값이 있어도 초기 상태로 안전 복구 |

## 5. 데이터 테스트

| 체크 | 항목 | 기준 |
|---|---|---|
| [ ] | `resources.json` 로딩 | 자원 데이터 정상 표시 |
| [ ] | `events.json` 로딩 | 구조화된 선택지 효과 정상 적용 |
| [ ] | `characters.json` 로딩 | 인물 데이터 정상 표시 |
| [ ] | `endings.json` 로딩 | 구조화된 엔딩 조건 정상 판정 |

## 6. 완료된 구조 개선

- [x] `data/events.json`과 `src/data.ts` 이벤트 중복 제거
- [x] `events.json` 선택지 효과 구조화
- [x] `endings.json` 엔딩 조건 구조화
- [x] localStorage 저장/불러오기 기능 추가

## 7. 다음 결함 후보

- 이벤트 발생 조건은 아직 문자열 중심이다.
- 엔딩 판정은 JSON 조건 기반으로 바뀌었지만, 우선순위 설계는 실제 플레이 테스트 후 재조정해야 한다.
- localStorage 저장은 브라우저 단일 기기 기준이므로, 계정 기반 저장은 추후 별도 설계가 필요하다.
