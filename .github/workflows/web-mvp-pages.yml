name: Deploy Web MVP Preview Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Branch Pages deployment is active
        run: echo "GitHub Pages uses branch deployment from main root. This workflow is intentionally manual only."
